package conversation

import (
	up "vox/internal/userProfile"
)

templ panel(userProfile *up.UserProfile){
<p id="info">Press space to start</p>
<div id="chat"></div>
<a id="end" href="#" class="d-none">End conversation</a>
}
