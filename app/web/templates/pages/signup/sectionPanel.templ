package signup
import (
    up "vox/internal/userProfile"
    "net/url"
)
templ panel(userProfile *up.UserProfile) {
    <div class="row justify-content-center">
        <div class="col-md-5 col-lg-4">
            <div class="card bg-dark border-secondary">
                <div class="card-header text-center py-3">
                    <h4 class="fw-bold text-indigo mb-0">Create Account</h4>
                </div>
                <div class="card-body p-4">
                    <form action="/process-signup" method="post">
                        <div class="mb-3">
                            <label class="form-label">First Name</label>
                            <input type="text" name="first_name" class="form-control" value={ url.QueryUnescape(userProfile.FirstName) } required/>
                        </div>
                        <div class="mb-3">
                            <label class="form-label">Last Name</label>
                            <input type="text" name="last_name" class="form-control" value={ url.QueryUnescape(userProfile.LastName) } required/>
                        </div>
                        <div class="mb-3">
                            <label class="form-label">Email</label>
                            <input type="email" name="email" class="form-control" value={ url.QueryUnescape(userProfile.Email) } required/>
                        </div>
                        <div class="mb-3">
                            <label class="form-label">Password</label>
                            <input type="password" name="password" class="form-control" required/>
                            <div class="form-text">≥8 chars, upper + lower, number & symbol.</div>
                        </div>
                        <div class="mb-4">
                            <label class="form-label">Repeat Password</label>
                            <input type="password" name="password-repeat" class="form-control" required/>
                        </div>
                        <button class="btn btn-secondary w-100">Sign Up</button>
                    </form>
                    <hr class="my-4">
                    <div class="d-grid">
                        <button class="btn btn-outline-light btn-sm d-flex align-items-center justify-content-center gap-2">
                            <img src="/web/static/images/google-logo.webp" width="20" height="20"/>
                            <span>Sign Up with Google</span>
                        </button>
                    </div>
                </div>
            </div>
        </div>
    </div>
}
