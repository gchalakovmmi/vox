services:
  app:
    image: vox-app
    build: ./app
    restart: unless-stopped
    env_file: .env
    ports: 
      - 8080:8080
  database:
    image: vox-database
    build: ./database
    restart: unless-stopped
